# 开发笔记

## 代码宗旨

All requirements are designed and implemented and demonstrate outstanding adherence to DRY and SOLID principles.
The design is flawless, with clear abstraction, modularity, and extensibility.
Design decisions reflect creative, effective, and maintainable solutions.

---

## 待新增

- [ ] MCP 服务
- [ ] **网页端界面简陋** — 当前只有两行 `<h1>` 和 `<a>`，无可视化操作

---

## 待修复 / 待优化

- [ ] **`dav_server.py` HEAD 请求调用不存在的方法** — `do_HEAD` 调用了 `get_contact()` / `get_event()`，实际应改为 `get_by_uid()`
- [ ] **拖拽导入绕过预览** — `app.py:handle_drop` 直接调用 `add_contact`/`add_event`，未使用预览对话框，与文件/URL/粘贴流程不一致
- [ ] **`config.py` 版本号未同步** — `SOFTWARE_VERSION = "2.0"`，实际已到 v2.1
- [ ] **`contacts_tab.py` / `calendar_tab.py` 大量重复代码** — `_import_selected`、`_import_file`、`_import_url`、`_import_clipboard`、`_import_data`、`show_text_import`、`show_import_preview` 几乎完全一致，违反 DRY 原则，应抽到 `BaseTreeTab`
- [ ] **`_import_data` 方法已成死代码** — 文件/URL/剪切板全部走预览，`_import_data` 没有被任何地方调用
- [ ] **`import_service.py` 已废弃** — `QueuedImportManager` / `TextImportManager` 全部未使用，旧导入流程已全部替换，应考虑移除
- [ ] **`event_dialog.py` 长达 1000 行** — `EventDialog` 违反 SRP，应拆分出提醒编辑器逻辑、RRULE 构造逻辑等
- [ ] **`base_tab.py` 缺少 Ctrl+点击的不连续多选** — 只有 Shift 连续多选，没有 Ctrl 不连续多选
- [ ] **`main.py` 缺少命令行参数** — 无法通过 `--db-path`、`--log-level`、`--port` 等参数启动
- [ ] **`progress_window.py` 取消时没有 grab_release** — 如果取消回调已经销毁窗口，可能造成父窗口卡死
- [ ] **`dav_server.py` `do_PUT`/`do_POST` 缺少 Content-Type 头**
- [ ] **`settings_dialog.py` 顶部导入了 `EventDialog` 和 `DetailedReminderEditor`** — 耦合过强，应只导入必要的映射常量
- [ ] **联系人 DAV 功能不完善** — 原"其他问题"中提及的继续完善
- [ ] **没有单元测试** — 没有任何 test 文件

---

## 其他想法

- MQTT 服务？

---

## 可能无法实现

- 实现当检测到有文件被拖动到窗口中的时候（鼠标还未释放）在窗口中醒目显示"拖拽至此以导入"字样

---

## 备注

- 尽量不限制或者指定窗口大小

---

## 更新日志

### v2.1

- **新增** 增加 SSL/TLS 配置设置
- **优化** 优化导入日程和联系人的逻辑
- **新增** 添加悬浮提示框

### v2.0

- **重构** 彻底重构代码方便后续扩展和维护
- **新增** 添加右键菜单
- **新增** 动态搜索功能
- **修复** 主窗口底部状态栏无法正常刷新
- **优化** 完善设置项

### v1.3

- **新增** 右键菜单
- **新增** 窗口菜单和"关于"信息
- **新增** 设置界面
- **新增** 列表排序
- **修复** 修复原始数据查看窗口数据会被意外截断换行

### v1.2

- **新增** 拖动多选、表头全/反选以及类复选框勾选
- **修复** 导出日程的时候会导出冗余的内容
- **优化** 日程的自定义重复设置会根据所选的日期自动变更（如果用户已经自定义过重复规则，即使开始日期变更也不会覆盖用户的设置）
- **优化** 事件状态逻辑
- **新增** 添加更多的导入方式

### v1.1

- **优化** 补全日志逻辑
- **修复** 拖拽导入失败
- **优化** 彻底完善日历处理
