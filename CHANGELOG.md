# 更新日志

## v2.2

- **新增** 集成 MCP（Model Context Protocol）服务，AI 可调用 16 个工具管理数据与服务端
- **新增** 设置界面「MCP 服务」标签页：启用/关闭、端口配置、连接 URL 提示、工具说明
- **新增** `MCPServer` 可嵌入类，以 SSE 协议在后台线程运行
- **新增** 底部状态栏显示 MCP 服务状态
- **优化** 勾选保存后动态启停 MCP 服务，无需重启程序
- **新增** `opencode.json` 项目配置，AI 开箱即用
- **新增** MCP 全部工具端到端测试 `tests/_run_mcp_tools_check.py`
- **文档** TECHNICAL.md 新增 MCP 服务器章节
- **修复** `update_status_bar` 中 `mcp_server` 实例在 `create_widgets` 之后才初始化导致的 AttributeError
- **新增** **统一鉴权系统** — 设置页「安全设置」可设置 PBKDF2-HMAC-SHA256 访问密码，WebDAV（Basic Auth）+ MCP（Bearer Token）共用
- **新增** **IP 访问控制** — 白名单（非空时仅允许名单内 IP）、黑名单、CIDR/通配符支持
- **新增** **鉴权日志** — 登录成功/失败、客户端 IP、协议标识、User-Agent 等可溯源信息，统一由 `AuthService.log_auth()` 入库
- **新增** **免密码 IP** — 本机（127.0.0.1/::1，可开关）和自定义 IP/CIDR 名单可绕过密码验证
- **优化** MCP 鉴权中间件 + WebDAV `_check_auth()` 均集成 `check_ip()` + `ip_bypasses_auth()` + `log_auth()`
- **优化** 复制令牌按钮添加 `EnhancedTooltip` 动态提示：未设密码时"请先设置密码以生成令牌"，已设时"复制令牌到剪贴板"

## v2.1

- **新增** 增加 SSL/TLS 配置设置
- **优化** 优化导入日程和联系人的逻辑
- **新增** 添加悬浮提示框
- **修复** 导入窗口取消后可能导致程序卡死的问题
- **修复** 某些情况下 WebDAV 无法正常返回数据的问题
- **修复** 拖拽导入时没有预览框就直接导入的问题
- **新增** 按 Ctrl 点击可以同时选中多条（不连续多选）
- **重构** 优化代码结构，减少重复代码
- **新增** 命令行参数启动（--port、--db-path、--log-level）
- **新增** 添加单元测试

## v2.0

- **重构** 彻底重构代码方便后续扩展和维护
- **新增** 添加右键菜单
- **新增** 动态搜索功能
- **修复** 主窗口底部状态栏无法正常刷新
- **优化** 完善设置项

## v1.3

- **新增** 右键菜单
- **新增** 窗口菜单和"关于"信息
- **新增** 设置界面
- **新增** 列表排序
- **修复** 修复原始数据查看窗口数据会被意外截断换行

## v1.2

- **新增** 拖动多选、表头全/反选以及类复选框勾选
- **修复** 导出日程的时候会导出冗余的内容
- **优化** 日程的自定义重复设置会根据所选的日期自动变更（如果用户已经自定义过重复规则，即使开始日期变更也不会覆盖用户的设置）
- **优化** 事件状态逻辑
- **新增** 添加更多的导入方式

## v1.1

- **优化** 补全日志逻辑
- **修复** 拖拽导入失败
- **优化** 彻底完善日历处理
